<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
  <title>대한민국 교통사고 차트</title>
	<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
  <script type="text/javascript">
    google.charts.load("current", {packages:["corechart"]});
    google.charts.setOnLoadCallback(drawChart);
    function drawChart() {
      var data = google.visualization.arrayToDataTable([
        {% for key, value in type_data.items() %}
          {% if value is string %}
            ['{{ key }}', '{{value[0]}}'],
          {% else %}
            ['{{ key }}', {{value[0]}}],
          {% endif %}
        {% endfor %}
      ]);

      var options = {
        title: '사고유형별 발생 비율',
        is3D: true,
      };

      var chart = new google.visualization.PieChart(document.getElementById('piechart_3d'));
      chart.draw(data, options);
    }
  </script>

  <script type="text/javascript">
    google.charts.load('current', {'packages':['corechart']});
    google.charts.setOnLoadCallback(drawVisualization);

    function drawVisualization() {
        // Some raw data (not necessarily accurate)
        var data = google.visualization.arrayToDataTable([
            ['사고유형', '발생건수', '사망자수','사망자 비율'],
            {% for type, value in death_cnt.items() %}
              {% if value is not string %}
                ['{{type}}', {{value[0]}}, {{value[1]}}, {{value[1]/value[0]}}],
              {% endif %}
            {% endfor %}
        ]);

        var options = {
            title : '사건 유형별 발생수와 사망자수',
            vAxis: {title: '횟수'},
            hAxis: {title: '유형'},
            seriesType: 'bars',
            vAxes: {
                1: {
                    title:'사망 비율',
                    textStyle: {color: 'red'}
                }
            },
            series: {
                1: {

                },
                2: {
                    targetAxisIndex:1,
                    type:'line'
                },
            width: 700,
            heigh: 700

            }

        };

        var chart = new google.visualization.ComboChart(document.getElementById('chart_div'));
        chart.draw(data, options);
    }
  </script>

</head>
<body>
	<div>
		<h3>사고유형별 발생 건수 와 사망자 비율을 알아보자.</h3>
		<div id="piechart_3d" style="width: 900px; height: 500px;"></div>
    <h3> 사고 유형은 보행노인이 가장 많았고, 스쿨존 어린이가 가장 적었다.</h2>
    <div id="chart_div" style="width: 1000px; height: 800px;"></div>
    <h3> 하지만, 사고 횟수는 적어도 사망자 비율이 가장 높은 사고는 무단횡단이였다.</h2>
	</div>
</body>
</html>